services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - notes_tmp:/tmp/notes-bot
    deploy:
      resources:
        limits:
          memory: 220m

volumes:
  notes_tmp:
